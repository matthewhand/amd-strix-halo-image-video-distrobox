# Slopfinity test/dev deps. Install on top of the runtime set:
#   pip install -r requirements-slopfinity.txt -r requirements-dev.txt
#
# The test suite (tests/) uses pytest with the asyncio plugin and httpx's
# ASGITransport to drive the FastAPI app in-process (see tests/conftest_server.py).
-r requirements-slopfinity.txt
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
pyflakes>=3.0
