[pytest]
asyncio_mode = auto
# Restrict collection to the real test dir so stray script-runners named
# test_*.py (e.g. scripts/test_wan_permutations.py, which makedirs /workspace
# at import) can't abort the whole suite during collection.
testpaths = tests
