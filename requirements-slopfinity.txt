# Slopfinity dashboard — runtime Python deps.
#
# Captured 2026-05-15 from the working host install. Lowerbound pins —
# tighten when you extract this to its own repo and want determinism.
#
#   pip install -r requirements-slopfinity.txt
#
# Slopfinity itself is imported as a package from the repo root (no
# editable install yet — that lands when the project is extracted).

fastapi>=0.136
uvicorn[standard]>=0.46
sqlmodel>=0.0.38
jinja2>=3.1
tiktoken>=0.12
python-dotenv>=1.2
