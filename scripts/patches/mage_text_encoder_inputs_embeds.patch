diff --git a/mage_flow/models/modules/text_encoder.py b/mage_flow/models/modules/text_encoder.py
index 660cbcb..92684b8 100644
--- a/mage_flow/models/modules/text_encoder.py
+++ b/mage_flow/models/modules/text_encoder.py
@@ -252,11 +252,12 @@ def model_forward(
         text_position_ids = position_ids[0]
 
     if kwargs.get("cu_seqlens") is None:
+        # transformers>=5.6 renamed input_embeds→inputs_embeds; >=5.12 dropped
+        # cache_position (deprecated/unused). Pass only kwargs that exist.
         attention_mask = create_causal_mask(
             config=self.config,
-            input_embeds=inputs_embeds,
+            inputs_embeds=inputs_embeds,
             attention_mask=attention_mask,
-            cache_position=cache_position,
             past_key_values=past_key_values,
             position_ids=text_position_ids,
         )
